services:
  - type: web
    name: vinitamart-frontend
    env: node
    buildCommand: npm install && npm run build
    staticPublishPath: "./build"
    envVars:
      - key: REACT_APP_API_URL
        sync: false  # Will be set to your backend URL